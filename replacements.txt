# Replacements to be made to the .content.html files when converting to html5

(< (abs
with
(&lt; (abs

<a name="(%_sec_.*)" \/>
with
<a name="\1"></a>
and
<a name="(.*)"></a>\n<h3>
with
<h3 id="\1">

<tt>
with 
<code>
and
</tt>
with
</code>

%_sec_
with
sec_

